# cleanup_branch_cleanup.yaml — branch cleanup configuration
#
# gate Rust cleanup validator reads this file for all policy decisions.

protected_branches:
  - main
  - master
  - dev
  - "release/*"

local_merged:
  action: WARN    # WARN | DELETE

remote_merged:
  action: WARN

orphan_local:
  action: WARN

temp_branch:
  action: WARN

temp_branch_prefixes:
  - "tmp/"
  - "wip/"
  - "test/"

current_branch_protected: true
