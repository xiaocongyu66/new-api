diff --git a/ui/apps/dashboard/src/App.tsx b/ui/apps/dashboard/src/App.tsx
index c07fa95..cbc95cd 100644
--- a/ui/apps/dashboard/src/App.tsx
+++ b/ui/apps/dashboard/src/App.tsx
@@ -17,6 +17,7 @@ limitations under the License.
 import { ClusterContext } from "@/hooks";
 import { useState } from "react";
 import "./App.css";
+import "./theme/dashboard-theme.css";
 import Router from "./routes";
 import { Helmet, HelmetProvider } from "react-helmet-async";
 import { ConfigProvider, App as AntdApp } from "antd";
@@ -24,6 +25,8 @@ import { StyleProvider } from "@ant-design/cssinjs";
 import { QueryClient, QueryClientProvider } from "@tanstack/react-query";
 import AuthProvider from "@/components/auth";
 import { getAntdLocale } from "@/utils/i18n.tsx";
+import { useParentTheme } from '@/components/parent-integration';
+import { theme as antdTheme } from 'antd';
 
 const queryClient = new QueryClient({
   defaultOptions: {
@@ -41,14 +44,53 @@ const queryClient = new QueryClient({
 
 function App() {
   const [cluster, setCluster] = useState<string>("");
+  const parentTheme = useParentTheme();
+  const isDark = parentTheme?.resolvedTheme === 'dark';
+  const token = (name: string, fallback: string) => parentTheme?.tokens?.[name] || fallback;
+  const radiusMap: Record<string, number> = { none: 0, sm: 5, md: 8, lg: 12, xl: 16, default: 8 };
+  const radius = radiusMap[parentTheme?.customization.radius || 'default'] ?? 8;
+  const fontFamily = parentTheme?.customization.font === 'serif'
+    ? 'Lora, Georgia, "Noto Serif SC", serif'
+    : 'Public Sans, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif';
   return (
     <StyleProvider layer>
       <ConfigProvider
         locale={getAntdLocale()}
         theme={{
+          algorithm: isDark ? antdTheme.darkAlgorithm : antdTheme.defaultAlgorithm,
+          token: {
+            colorPrimary: token('primary', '#1677ff'),
+            colorInfo: token('info', '#1677ff'),
+            colorSuccess: token('success', '#52c41a'),
+            colorWarning: token('warning', '#faad14'),
+            colorError: token('destructive', '#ff4d4f'),
+            colorBgBase: token('background', '#ffffff'),
+            colorBgLayout: token('background', '#ffffff'),
+            colorBgContainer: token('card', '#ffffff'),
+            colorBgElevated: token('popover', '#ffffff'),
+            colorText: token('foreground', '#171717'),
+            colorTextSecondary: token('muted-foreground', '#666666'),
+            colorBorder: token('border', '#d9d9d9'),
+            colorBorderSecondary: token('border', '#f0f0f0'),
+            colorLink: token('primary', '#1677ff'),
+            controlOutline: token('ring', '#1677ff'),
+            fontFamily,
+            borderRadius: radius,
+            borderRadiusSM: Math.max(0, Math.round(radius * 0.75)),
+            borderRadiusLG: Math.round(radius * 1.5),
+            fontSize: parentTheme?.customization.scale === 'xl' ? 16 : parentTheme?.customization.scale === 'lg' ? 15 : parentTheme?.customization.scale === 'sm' ? 13 : 14,
+          },
           components: {
             Layout: {
-              siderBg: "#ffffff",
+              siderBg: token('sidebar', isDark ? '#111827' : '#ffffff'),
+              bodyBg: token('background', isDark ? '#111827' : '#ffffff'),
+              headerBg: token('card', isDark ? '#111827' : '#ffffff'),
+            },
+            Menu: {
+              itemBg: token('sidebar', isDark ? '#111827' : '#ffffff'),
+              itemColor: token('sidebar-foreground', isDark ? '#f5f5f5' : '#171717'),
+              itemSelectedBg: token('sidebar-accent', isDark ? '#1f2937' : '#eff6ff'),
+              itemSelectedColor: token('sidebar-accent-foreground', '#1677ff'),
             },
           },
         }}
diff --git a/ui/apps/dashboard/src/components/auth/index.tsx b/ui/apps/dashboard/src/components/auth/index.tsx
index cbdc9b6..ca8b38e 100644
--- a/ui/apps/dashboard/src/components/auth/index.tsx
+++ b/ui/apps/dashboard/src/components/auth/index.tsx
@@ -27,6 +27,7 @@ import { karmadaClient } from '@/services';
 import { useQuery } from '@tanstack/react-query';
 import { karmadaMemberClusterClient } from "@/services/base.ts";
 import { metricsScraperClient } from "@/services/metrics.ts";
+import { isEmbeddedSessionMode } from '@/components/parent-integration';
 
 export interface AuthUser {
   name?: string;
@@ -50,38 +51,37 @@ const AuthContext = createContext<{
 });
 
 const AuthProvider = ({ children }: { children: ReactNode }) => {
-  const [token, setToken_] = useState(localStorage.getItem('token') || '');
+  const [token, setToken_] = useState(
+    isEmbeddedSessionMode ? '__session__' : localStorage.getItem('token') || '',
+  );
   const setToken = useCallback((newToken: string) => {
+    if (isEmbeddedSessionMode) return;
     localStorage.setItem('token', newToken);
     setToken_(newToken);
   }, []);
   const logout = useCallback(() => {
-    localStorage.removeItem('token');
-    sessionStorage.removeItem('oidc_state');
+    if (!isEmbeddedSessionMode) {
+      localStorage.removeItem('token');
+      sessionStorage.removeItem('oidc_state');
+    }
     delete karmadaClient.defaults.headers.common['Authorization'];
     delete karmadaMemberClusterClient.defaults.headers.common['Authorization'];
-    setToken_('');
+    delete metricsScraperClient.defaults.headers.common['Authorization'];
+    setToken_(isEmbeddedSessionMode ? '__session__' : '');
   }, []);
   const { data, isLoading } = useQuery({
     queryKey: ['Me', token],
     queryFn: async () => {
-      if (token) {
-        karmadaClient.defaults.headers.common[
-          'Authorization'
-        ] = `Bearer ${token}`;
-        karmadaMemberClusterClient.defaults.headers.common[
-          'Authorization'
-        ] = `Bearer ${token}`;
-        metricsScraperClient.defaults.headers.common[
-          'Authorization'
-        ] = `Bearer ${token}`;
-        const ret = await Me();
-        return ret.data as MeResponse;
-      } else {
-        return {
-          authenticated: false,
-        } as MeResponse;
+      if (!token) {
+        return { authenticated: false } as MeResponse;
+      }
+      if (!isEmbeddedSessionMode) {
+        karmadaClient.defaults.headers.common['Authorization'] = `Bearer ${token}`;
+        karmadaMemberClusterClient.defaults.headers.common['Authorization'] = `Bearer ${token}`;
+        metricsScraperClient.defaults.headers.common['Authorization'] = `Bearer ${token}`;
       }
+      const ret = await Me();
+      return ret.data as MeResponse;
     },
   });
   const ctxValue = useMemo(() => {
diff --git a/ui/apps/dashboard/src/components/panel/index.tsx b/ui/apps/dashboard/src/components/panel/index.tsx
index 7160375..124f78f 100644
--- a/ui/apps/dashboard/src/components/panel/index.tsx
+++ b/ui/apps/dashboard/src/components/panel/index.tsx
@@ -55,7 +55,7 @@ const Panel: FC<IPanelProps> = (props) => {
   }, [matches]);
   return (
     <div className="w-full h-full px-[30px] py-[20px] box-border bg-[#FAFBFC]">
-      <div className="w-full h-full bg-white box-border p-[12px] overflow-y-scroll">
+      <div className="w-full h-full bg-white box-border p-[12px] overflow-y-auto">
         <Breadcrumb className="mb-4" items={breadcrumbs} />
         {children}
       </div>
diff --git a/ui/apps/dashboard/src/layout/index.tsx b/ui/apps/dashboard/src/layout/index.tsx
index 3cd191a..b8ee636 100644
--- a/ui/apps/dashboard/src/layout/index.tsx
+++ b/ui/apps/dashboard/src/layout/index.tsx
@@ -53,7 +53,7 @@ export const MainLayout: FC = () => {
     <>
       <Header onTerminalClick={toggleKarmadaTerminal} user={user} onLogout={logout} />
       <AntdLayout
-        className={cn('h-[calc(100vh-48px)]', 'overflow-hidden', 'flex')}
+        className={cn('h-[calc(100dvh-48px)]', 'min-h-0', 'overflow-hidden', 'flex')}
       >
         <AntdSider
           width={getSidebarWidth()}
@@ -64,8 +64,10 @@ export const MainLayout: FC = () => {
         >
           <Sidebar collapsed={isSmallScreen} />
         </AntdSider>
-        <AntdContent>
-          <Outlet />
+        <AntdContent className={cn('min-h-0', 'min-w-0', 'overflow-hidden')}>
+          <div className={cn('h-full', 'min-h-0', 'overflow-y-auto')}>
+            <Outlet />
+          </div>
         </AntdContent>
       </AntdLayout>
